import { StoryblokComponent } from 'storyblok-js-client';

export interface SwitcherBehaviorToggleSubModule
	extends StoryblokComponent<'switcher_behavior_toggle_sub_module'> {
	margin_bottom: string;
}
