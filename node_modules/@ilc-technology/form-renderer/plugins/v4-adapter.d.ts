import { IForm } from '..';
export declare const PLUGIN_LISTENER_NAME = "Forms API v4 Adapter";
interface IPQEntry {
    Question: string;
    Answer: string;
}
interface ReservationDetails {
    [key: string]: string | number | boolean;
}
interface ILegacyForm extends IForm {
    pqData?: IPQEntry[];
    setPriceQuotationData?: (pqData: IPQEntry[]) => void;
    infomeetingId?: string;
    setInfomeetingId?: (infomeetingId: string) => void;
    setReservationDetails?: (reservationDetails: ReservationDetails) => void;
    reservationDetails?: ReservationDetails;
}
declare const callback: (form: ILegacyForm) => void;
export default callback;
