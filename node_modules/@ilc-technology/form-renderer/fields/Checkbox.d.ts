import { IForm } from '..';
import Field, { IFieldConfiguration } from './Field';
export default class Checkbox extends Field<boolean> {
    configuration: IFieldConfiguration<boolean>;
    form: IForm;
    static builder(conf: IFieldConfiguration<boolean>, form: IForm): Checkbox;
    htmlElement?: HTMLElement;
    _inputElement?: HTMLInputElement;
    _previousValue: boolean;
    private constructor();
    get value(): boolean;
    set value(value: boolean);
    get previousValue(): boolean;
    render: () => HTMLElement;
    triggerChangeEvent(): void;
}
