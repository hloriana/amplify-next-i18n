import { IForm } from '..';
import Field, { IFieldConfiguration } from './Field';
export default class Hidden extends Field<string> {
    configuration: IFieldConfiguration<string>;
    form: IForm;
    static builder(conf: IFieldConfiguration<string>, form: IForm): Hidden;
    htmlElement?: HTMLElement;
    _inputElement: HTMLInputElement | null;
    private constructor();
    get value(): string;
    set value(value: string);
    get previousValue(): string;
    render: () => HTMLElement;
}
