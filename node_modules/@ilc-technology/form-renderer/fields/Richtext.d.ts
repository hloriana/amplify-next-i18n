import { IForm } from '..';
import Field, { IFieldConfiguration } from './Field';
export default class Richtext extends Field<string> {
    configuration: IFieldConfiguration<string>;
    form: IForm;
    static builder(conf: IFieldConfiguration<string>, form: IForm): Richtext;
    htmlElement?: HTMLElement;
    private constructor();
    get value(): string;
    set value(value: string);
    get previousValue(): string;
    render: () => HTMLElement;
}
