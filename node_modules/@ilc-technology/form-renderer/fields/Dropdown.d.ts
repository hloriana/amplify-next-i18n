import { IForm } from '..';
import Field, { IFieldConfiguration, IFieldConfigurationWithOptions } from './Field';
export default class NativeDropdown extends Field<string> {
    configuration: IFieldConfigurationWithOptions<string>;
    form: IForm;
    static builder(conf: IFieldConfiguration<string>, form: IForm): NativeDropdown;
    htmlElement?: HTMLElement;
    _selectElement: HTMLSelectElement | null;
    _previousValue: string;
    private constructor();
    get value(): string;
    set value(value: string);
    get previousValue(): string;
    hideOptions(optionCodes: string[]): void;
    showOptions(optionCodes: string[]): void;
    render: () => HTMLElement | null;
}
