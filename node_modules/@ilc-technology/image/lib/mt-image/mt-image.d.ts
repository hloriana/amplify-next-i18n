/// <reference types="react" />
import Image, { ImageProps } from 'next/image';
import { Size } from '../utils';
declare type MTImageProps = Omit<ImageProps, 'src' | 'width' | 'height'> & {
    imageUrl: string;
    alt: string;
    image: typeof Image;
    size?: Size;
    wrapperClassname?: string;
};
export declare const MtImage: ({ imageUrl, size, alt, wrapperClassname, image: ImageComp, ...all }: MTImageProps) => JSX.Element;
export {};
