const withLinariaMetadata = value => value && typeof value.linaria === 'object';

export default {
  test: withLinariaMetadata,
  print: ({
    linaria
  }) => `
CSS:

${Object.keys(linaria.rules).map(selector => `${selector} {${linaria.rules[selector].cssText}}`).join('\n')}

Dependencies: ${linaria.dependencies.length ? linaria.dependencies.join(', ') : 'NA'}
`
};
//# sourceMappingURL=linaria-snapshot-serializer.js.map